# raw — ElevenLabs 원본 받는 자리

`../sfx_elevenlabs_prompts.md` 의 프롬프트 A/B/C 로 생성한 원본을 **파일명 그대로** 여기에 넣습니다.
프롬프트당 4변형 = 12개. 이름을 바꾸면 어느 프롬프트에서 나왔는지 추적이 안 됩니다.

여기 파일은 최종 리소스가 아닙니다. `build_sfx.py` 의 ② 텍스처 단계에서
**재질 구간만 잘라내** 확정된 엔벨로프에 얹은 결과가 `../SFX_Ball_*.wav` 입니다.
