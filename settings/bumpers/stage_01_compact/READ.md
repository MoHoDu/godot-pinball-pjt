# Stage 01 축소 범퍼 설정

메뉴 경로: `Pinball > Game Settings > Bumpers > stage_01_compact/...`

축소·컴팩트 보드에서 사용하는 범퍼 크기와 충돌 수치 변형입니다. 프로덕션 기본값은 `stage_01` 폴더이며 이 폴더는 작은 레이아웃을 명시적으로 사용하는 씬에만 적용합니다.

기본 범퍼를 수정해도 이 파일들은 자동으로 따라가지 않습니다. 두 레이아웃의 게임 감각을 같이 유지하려면 같은 종류 파일을 나란히 비교하세요.
